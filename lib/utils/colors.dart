import 'package:flutter/material.dart';

/// ค่าสีที่ใช้ในธีมของแอปทั้งหมด
class AppColors {
  /// สีหลักของแอป
  static const Color primary = Color(0xFF1F7A6C);

  /// สีหลักที่อ่อนลงสำหรับพื้นหลังหรือไอเท็มเลือก
  static const Color primarySoft = Color(0xFFDDF3EC);

  /// สีเสริมสำหรับไอเท็มสำคัญหรือการเน้น
  static const Color accent = Color(0xFFF4B860);

  /// สีพื้นหลังหลักของแอป
  static const Color background = Color(0xFFF4F7F2);

  /// สีพื้นผิวหลักของการ์ดและกล่อง
  static const Color surface = Colors.white;

  /// สีพื้นผิวอ่อนสำหรับพื้นที่รอง
  static const Color surfaceMuted = Color(0xFFEAF0E8);

  /// สีเส้นขอบของการ์ดและกล่อง
  static const Color border = Color(0xFFD9E3D8);

  /// สีข้อความหลัก
  static const Color textPrimary = Color(0xFF16221F);

  /// สีข้อความรอง
  static const Color textSecondary = Color(0xFF6A7B76);

  /// สีแสดงสถานะสำเร็จ
  static const Color success = Color(0xFF2E9D6D);

  /// สีแสดงสถานะเตือน
  static const Color warning = Color(0xFFF0A64B);

  /// สีแสดงสถานะผิดพลาดหรือยกเลิก
  static const Color danger = Color(0xFFDE6B6B);

  /// สีเงาในการแสดงผลแบบการ์ด
  static const Color shadow = Color(0x140A1F18);
}
